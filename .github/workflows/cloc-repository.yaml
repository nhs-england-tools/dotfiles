name: Count lines of code of this repository

on:
  workflow_call:

jobs:
  cloc-repository:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - run: |
          export FORMAT=default
          ./scripts/githooks/cloc-repository.sh
